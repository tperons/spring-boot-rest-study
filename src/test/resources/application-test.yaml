cors:
    originPatterns: ${CORS_ORIGIN_PATTERNS:http://localhost:8080}
file:
    upload-dir: ${java.io.tmpdir}/meu-projeto-uploads-test
security:
    jwt:
        token:
            secret-key: ${JWT_SECRET:GgNaO2}
            expire-length: ${JWT_EXPIRE:3600000}
            refresh-expire-length: ${JWT_REFRESH_EXPIRE:18000000}
seed:
    admin:
        password: ${SEED_ADMIN_PASSWORD:HioiGa0H}
spring:
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                open-in-view: false
    mail:
        username: ${EMAIL_USERNAME:email@email.com}
        password: ${EMAIL_PASSWORD:QppGUGo6}

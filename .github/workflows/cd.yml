name: CD - Docker Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 25
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "25"
          cache: maven

      - name: Build Application .jar
        run: mvn clean verify

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker Build Image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/spring-rest-api:latest .

      - name: Push to Docker Hub Container Registry
        run: |
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/spring-rest-api:latest ${{ secrets.DOCKERHUB_USERNAME }}/spring-rest-api:${{ github.ref_name }}

          docker push ${{ secrets.DOCKERHUB_USERNAME }}/spring-rest-api:${{ github.ref_name }}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/spring-rest-api:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Execute commands via SSH on VPS
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /root/apis/spring-rest-api
            export APP_VERSION=${{ github.ref_name }}
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
